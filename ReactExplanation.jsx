import React from 'react';

const ReactExplanation = () => {
  return (
    <div>
      <h1>What is React?</h1>
      <p>React is a JavaScript library for building user interfaces, developed by Facebook.</p>
      <p>It uses a component-based architecture where UIs are built from reusable pieces.</p>
      <p>React uses JSX, a syntax extension that allows writing HTML-like code in JavaScript.</p>
      <p>The virtual DOM ensures efficient updates by only re-rendering changed components.</p>
      <p>State management and props enable dynamic, interactive applications.</p>
      <p>React's declarative approach makes code predictable and easier to debug.</p>
    </div>
  );
};

export default ReactExplanation;
