import React from 'react';

// React is a JavaScript library for building user interfaces
// It uses components - reusable pieces of UI that manage their own state
// JSX allows you to write HTML-like syntax directly in JavaScript
// Components can be functional (like this one) or class-based
// React efficiently updates the DOM using a virtual DOM diffing algorithm
// Props pass data down from parent to child components
// State manages data that changes over time within a component
// Hooks like useState and useEffect add functionality to functional components

const ReactExplanation = () => {
  return (
    <div>
      <h1>React: A declarative, component-based library for building UIs</h1>
    </div>
  );
};

export default ReactExplanation;
