import React from "react";

function ExplainReact() {
  return (
    <section>
      <h1>What is React?</h1>
      <p>React is an open-source JavaScript library created by Meta for building user interfaces.</p>
      <p>It uses a component-based architecture, letting you split UI into reusable, independent pieces.</p>
      <p>React employs a Virtual DOM to efficiently update only the parts of the page that change.</p>
      <p>State and props drive dynamic rendering, making interactive UIs simple and declarative.</p>
      <p>Its rich ecosystem — including React Router, Redux, and Next.js — powers modern web apps at scale.</p>
    </section>
  );
}

export default ExplainReact;
