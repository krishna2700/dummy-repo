// WhatIsReact.jsx - A brief explanation of React

function WhatIsReact() {
  // React is a JavaScript library for building user interfaces, developed by Facebook.
  // It uses a component-based architecture, letting you build reusable UI pieces.
  // React introduces JSX, a syntax that blends HTML-like markup directly into JavaScript.
  // The Virtual DOM makes updates efficient by only re-rendering what actually changed.
  // State and props allow components to manage and share data reactively.
  // Hooks like useState and useEffect let functional components handle logic and side effects.
  // React's ecosystem (React Router, Redux, Next.js) makes it ideal for full-scale apps.

  return (
    <div>
      <h1>What is React?</h1>
      <p>React is a JavaScript library for building fast, interactive user interfaces using reusable components.</p>
    </div>
  );
}

export default WhatIsReact;
